# 1. 프로그램 설계서 (5점)

## 1. 프로젝트 주제
도서 관리 프로그램
도서 관리 프로그램

## 2. 핵심 기능 목록
(구현할 기능들을 순서대로 나열하세요.)
1.  도서 추가: 새로운 도서를 목록에 추가합니다.
2.  도서 삭제: 도서 목록에서 특정 도서를 삭제합니다.
3.  도서 검색: 도서 이름이나 저자 이름으로 도서를 검색합니다.
4.  도서 수정: 도서의 정보를 수정합니다.
5.  도서 목록 출력: 현재 저장된 도서 목록을 출력합니다.
1.  도서 추가: 새로운 도서를 목록에 추가합니다.
2.  도서 삭제: 도서 목록에서 특정 도서를 삭제합니다.
3.  도서 검색: 도서 이름이나 저자 이름으로 도서를 검색합니다.
4.  도서 수정: 도서의 정보를 수정합니다.
5.  도서 목록 출력: 현재 저장된 도서 목록을 출력합니다.

## 3. 데이터 구조 설계
도서 관리 시스템은 도서 이름, 저자, 출판년도, 장르 등의 정보를 다루게 됩니다. 이러한 정보를 저장하기 위해 배열을 사용할 수 있습니다.
도서 관리 시스템은 도서 이름, 저자, 출판년도, 장르 등의 정보를 다루게 됩니다. 이러한 정보를 저장하기 위해 배열을 사용할 수 있습니다.

**방법 1: 배열 사용 (기본)**
***c
// 도서 5권의 이름, 저자, 출판년도, 장르 정보를 저장
char bookNames[5][100];    // 도서 이름
char authors[5][50];       // 저자 이름
int publishYears[5];       // 출판 년도
char genres[5][30];        // 장르
int count = 0;             // 현재 도서 수

***c
// 도서 5권의 이름, 저자, 출판년도, 장르 정보를 저장
char bookNames[5][100];    // 도서 이름
char authors[5][50];       // 저자 이름
int publishYears[5];       // 출판 년도
char genres[5][30];        // 장르
int count = 0;             // 현재 도서 수


**방법 2: 구조체 사용 (선택)**
```c
// 도서 정보 구조체 정의
struct Book {
    char name[100];        // 도서 이름
    char author[50];       // 저자 이름
    int publishYear;       // 출판 년도
    char genre[30];        // 장르
// 도서 정보 구조체 정의
struct Book {
    char name[100];        // 도서 이름
    char author[50];       // 저자 이름
    int publishYear;       // 출판 년도
    char genre[30];        // 장르
};

// 도서 5권 정보를 저장하는 배열
struct Book books[5];     // 최대 5권의 도서 정보를 저장
int count = 0;            // 현재 도서 수
